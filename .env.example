# Free key from https://api.nasa.gov (DEMO_KEY allows about 10 requests per hour per IP).
# Copy to .env, which git ignores. On Netlify, set it under Environment variables.
VITE_NASA_API_KEY=DEMO_KEY
